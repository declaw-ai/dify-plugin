identity:
  name: kill_sandbox
  author: declaw
  label:
    en_US: Kill Sandbox

description:
  human:
    en_US: Destroy a Declaw sandbox and release its resources.
  llm: Destroy a sandbox and release its resources.

parameters:
  - name: sandbox_id
    type: string
    required: true
    label:
      en_US: Sandbox ID
    llm_description: Sandbox ID to destroy.
    form: llm

extra:
  python:
    source: tools/kill_sandbox.py
